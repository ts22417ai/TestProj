
@{
    ViewBag.Title = "儲值成功";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}


<div class="contact-area section-padding-0-80">
    <div class="container">
        <div class="row">
            <div class="col-12 text-center mt-5">
                <span>將於<span id="seconds"></span>秒後為您跳轉至交易紀錄頁面。</span><br><br>
                <span>
                    @* 移至會員中心 *@
                    或者自行點擊<a href="@Url.Action("", "")" class="single-contact-information">
                        <h6>交易紀錄</h6>
                    </a>
                </span>
            </div>
        </div>
    </div>
</div>

@section scripts {

    <script>
        {
            let t = 6;
            function reciprocal() {
                t -= 1;
                document.getElementById('seconds').innerHTML = t;
                if (t == 0) {
                    @* 移至會員中心 *@
                    location.href = "@Url.Action("", "")"; //之後交易紀錄好要改連結
                }
                // 不能在 IIFE 裡,會找不到 func
                setTimeout("reciprocal()", 1000);
            }
            window.onload = reciprocal;
        }
    </script>
}